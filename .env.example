# Paste your Slack incoming webhook, Zapier webhook, Make.com, n8n, or any HTTP POST URL here.
# Leave blank to disable webhook forwarding (leads are still logged to server console).
WEBHOOK_URL=

# Optional: A shared secret added as a Bearer token when calling the webhook.
# Useful if your webhook endpoint requires authorization.
WEBHOOK_SECRET=
